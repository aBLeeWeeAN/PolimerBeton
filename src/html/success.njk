{% extends "base/base.njk" %}

<!-- ? передаем ветку из ru.json для этой страницы -->
{% set current_page_head = success.head %}

{% block content %}
    <div class="my-shadow-group">
        <section id="success" class="my-server-response">
            <div class="container my-wrapper my-server-response__content">
                <div class="my-balls-group my-balls-group--decorative " data-variant="usage" aria-hidden="true">
                    {%
                        set usage__balls__grid_classes = [
                            "d-lg-inline-block",
                            "d-lg-inline-block",
                            "d-lg-inline-block",
                            "d-xl-inline-block",
                            "d-xl-inline-block"
                        ]
                    %}

                    {% for ball in my_balls %}
                        <img
                            data-my-picture-class="d-none {{ usage__balls__grid_classes[loop.index0] }} my-balls-group__picture my-balls-group__picture--{{ ball }}"
                            class="my-balls-group__img"
                            src="@images/balls/{{ ball }}.png"
                            alt="" />
                    {% endfor %}
                </div>

                <h1 class="my-title my-title--server-response" data-variant="success">{{ success.main_content.heading }}</h1>
                <h2 class="my-subtitle my-subtitle--server-response">{{ success.main_content.subheading | nl2br }}</h2>

                <div class="my-default-button">
                    <a class="my-default-button__link" href="@page/index.html">{{ success.main_content.button_text }}</a>
                </div>
            </div>
        </section>
    </div>
{% endblock %}
